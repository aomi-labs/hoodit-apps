# Hoodit portfolio
Read an explicit public wallet. Resolve the connected funded executor with `get_account_info`. A portfolio response is one Blockscout provider page; follow its cursor explicitly. Request quotes only when needed. For a fractional sell, get exact sizing with `include_quote=false`, then pass the formatted amount to inherited LI.FI preparation.
