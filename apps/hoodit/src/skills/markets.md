# Hoodit markets
Use the five market reads for public Robinhood Chain research. Search returns candidates rather than resolving ambiguous symbols. Token, candle, and trade tools require an exact token contract. Label history and activity as single-pool coverage. Never turn a read into a transaction instruction.
