{
  "user_story": "Research a Robinhood Chain token and inspect a public wallet with Hoodit's read-only v1 tools without preparing a transaction.",
  "wallet_seed": { "address": "0xb202bb725c85b90bd847d350ebc7f16ff8408ed8", "chain_id": 4663, "is_connected": true },
  "turns": [
    { "prompt": "Find PONS token candidates on Robinhood Chain. Show exact contracts and do not choose an ambiguous symbol for me.", "expected_tools": { "must_call": ["activate_skills", "hoodit_search_tokens"] } },
    { "prompt": "Using the exact PONS contract from that result, show its token stats and selected pool context.", "expected_tools": { "must_call": ["hoodit_get_token"] } },
    { "prompt": "Show the last five completed 15-minute candles for that exact token and selected pool.", "expected_tools": { "must_call": ["hoodit_get_candles"] } },
    { "prompt": "Read the seeded public wallet's current portfolio balances only. Do not request valuation quotes and do not prepare a trade.", "expected_tools": { "must_call": ["activate_skills", "hoodit_get_portfolio"] } },
    { "prompt": "Read that wallet's exact USDG holding and calculate the exact atomic and formatted half-balance size without a quote.", "expected_tools": { "must_call": ["hoodit_get_holding"] } }
  ],
  "final_assertion": { "user_state": { "pending_txs": { "max_count": 0 }, "pending_eip712s": { "max_count": 0 } }, "no_errors": true, "max_turns": 10 }
}
